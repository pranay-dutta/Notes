- 📝 **TDD** is a way of **building software** which follows `@3 steps cycle.`
	1. Start by writing **failing test**
	2. Write just **enough code** to make the **test pass**
	3. **Refactor** if necessary
	
→ [[Unit testing in JavaScript]]